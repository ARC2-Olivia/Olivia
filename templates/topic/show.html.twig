{% extends 'base.html.twig' %}
{% trans_default_domain 'app' %}

{% block title %}{{ 'page.topic.show'|trans }}{% endblock %}

{% block body %}
    <div class="index-banner" style="background-image: url('{{ asset('build/images/bg-module-theory.png') }}'); background-position: center 60%">
        <div class="index-banner-wrapper">
            <div class="index-banner-title">{{ 'app.brand.short'|trans }}</div>
            <div class="index-banner-subtitle">{{ 'nav.topics'|trans }}</div>
        </div>
    </div>
    <div class="container py-5">
        {% include "_partials/toasts.html.twig" with {'classAddition': 'row'} %}
        <div class="row g-3">
            <div class="topic-title">{{ topic.title }}</div>
            <img class="learn2apply-arrow" src="{{ asset('build/images/smudged-arrow.png') }}" alt="Learn to start applying"/>
            <div class="topic-segments">

                <div class="topic-segment theory">
                    <div class="topic-segment-grid">
                        {% for course in theoreticalSubmodules %}
                            {% include 'course/_index_course.html.twig' with {'usePublicName': true} %}
                        {% endfor %}
                    </div>
                </div>

                <div class="topic-segments-divider"></div>

                <div class="topic-segment">
                    <div class="topic-segment-grid">
                        {% for evaluation in practicalSubmodules %}
                            {% include 'evaluation/_index_evaluation.html.twig' with {'usePublicName': true} %}
                        {% endfor %}
                    </div>
                </div>

            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        window.addEventListener("DOMContentLoaded", () => {
            const cards = document.querySelectorAll(".card");
            let maxHeight = 0;
            for (const card of cards) {
                if (maxHeight < card.offsetHeight) {
                    maxHeight = card.offsetHeight;
                }
            }
            for (const card of cards) {
                card.style.height = `${maxHeight}px`;
            }
        });
    </script>
{% endblock %}